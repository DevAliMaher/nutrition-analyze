import { RemoveCountPipe } from './remove-count.pipe';

describe('RemoveCountPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveCountPipe();
    expect(pipe).toBeTruthy();
  });
});
